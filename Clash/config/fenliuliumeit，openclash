[custom]

; 规则集配置（最简单，确保规则里用到的代理组存在）
ruleset=局域网,rules/ACL4SSR/Clash/LocalAreaNetwork.list
ruleset=默认直连,[]FINAL

; 代理组定义，必须包含规则里用的“局域网”组
custom_proxy_group=局域网`select`DIRECT
custom_proxy_group=自动选择`url-test`自动选择.*`http://www.gstatic.com/generate_204`300,,50
custom_proxy_group=全球直连`select`DIRECT
custom_proxy_group=节点选择`select`自动选择

; 启用规则生成器，覆盖原规则，自动生成代理组
enable_rule_generator=true
overwrite_original_rules=true
